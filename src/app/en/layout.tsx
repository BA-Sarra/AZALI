import { LocalizedShell } from '@/components/site/LocalizedShell';
export default function EnLayout({ children }: { children: React.ReactNode }) { return <LocalizedShell locale="en">{children}</LocalizedShell>; }
