# Micro-typography fixes

Applied requested homepage refinements:
- Hero headline split into two non-wrapping desktop lines: 62px then 48px.
- Hero support copy set to 16px.
- Notebook Holder descriptor split into two lines.
- Why AZALI body copy reduced to fit clean two-line blocks.
- Philosophy heading split into two exact lines: 34px then 50px.
- Philosophy body copy split into three 15px lines.
- Arabic AZALI watermark set to 86px, sand #C4A06E, 18% opacity.
