# Hover fix

The garnet hover is now enforced at CSS level through `.hover-card-garnet:hover` instead of relying only on Tailwind generated hover utilities. Product and category cards also use image overlays so the full card visibly turns garnet, including the image area.
